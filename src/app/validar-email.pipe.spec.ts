import { ValidarEmailPipe } from './validar-email.pipe';

describe('ValidarEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidarEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
